'use client';

/** Auth pages inherit localization from the root `ClerkRootProvider`. */
export function ClerkAuthLocalizationProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
